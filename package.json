{
  "name": "pi-persona",
  "version": "0.1.0",
  "description": "为 pi 注入拟人化情感性格 — pi 会开心、会沮丧、会傲娇、会跟你建立默契",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsc --outDir .tmp-test --noEmit false && node --test .tmp-test/src/**/*.test.js"
  },
  "keywords": ["pi-package", "personality", "mood", "emotion"],
  "author": "",
  "license": "MIT",
  "pi": {
    "extensions": ["./extensions"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "files": [
    "src",
    "extensions"
  ],
  "dependencies": {
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
